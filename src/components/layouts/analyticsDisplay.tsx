import { Counter } from "@/components/layouts";  // Importing the Counter component for analytics

/**
 * This section includes the StatsCounter component.
 */
export const AnalyticsDisplay = () => {
  return (
    <div className="mt-8">
      <Counter />
    </div>
  );
};